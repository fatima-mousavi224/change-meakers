import { createInitiativePage } from "@/lib/initiativeRoutePage";

const { generateMetadata, Page } = createInitiativePage(
  "additional-learning-programs",
);

export { generateMetadata };
export default Page;
